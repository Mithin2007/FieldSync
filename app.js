const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const PATHS={home:'<path d="M3 11.5 12 4l9 7.5"/><path d="M5 10v10h14V10M9 20v-6h6v6"/>',clipboard:'<rect x="5" y="4" width="14" height="17" rx="2"/><path d="M9 4V2h6v2M9 9h6M9 13h6M9 17h4"/>',asset:'<path d="m12 3 8 4.5v9L12 21l-8-4.5v-9z"/><path d="m4 7.5 8 4.5 8-4.5M12 12v9"/>',sync:'<path d="M20 7h-5V2"/><path d="M20 7a8 8 0 0 0-14-2M4 17h5v5"/><path d="M4 17a8 8 0 0 0 14 2"/>',users:'<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>',building:'<rect x="3" y="9" width="18" height="12" rx="1"/><path d="M9 21V3h6v18M6 13h1M6 17h1M17 13h1M17 17h1M11 7h2"/>',report:'<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6M8 13h8M8 17h8M8 9h2"/>',alert:'<path d="M10.3 2.9 1.8 17a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 2.9a2 2 0 0 0-3.4 0Z"/><path d="M12 9v4M12 17h.01"/>',history:'<path d="M3 12a9 9 0 1 0 3-6.7L3 8"/><path d="M3 3v5h5M12 7v5l3 2"/>',settings:'<circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.7 1.7 0 0 0 .3 1.9l.1.1-2.8 2.8-.1-.1a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.6v.2h-4V21a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.3l-.1.1L4.2 17l.1-.1a1.7 1.7 0 0 0 .3-1.9A1.7 1.7 0 0 0 3 14H2.8v-4H3a1.7 1.7 0 0 0 1.6-1 1.7 1.7 0 0 0-.3-1.9L4.2 7 7 4.2l.1.1A1.7 1.7 0 0 0 9 4.6a1.7 1.7 0 0 0 1-1.6v-.2h4V3a1.7 1.7 0 0 0 1 1.6 1.7 1.7 0 0 0 1.9-.3l.1-.1L19.8 7l-.1.1a1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0 1.6 1h.2v4H21a1.7 1.7 0 0 0-1.6 1Z"/>',bell:'<path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4"/>',search:'<circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/>',user:'<circle cx="12" cy="8" r="4"/><path d="M4 21a8 8 0 0 1 16 0"/>',wifiOff:'<path d="m2 2 20 20M8.5 8.5A9 9 0 0 1 21 9M5 9a12 12 0 0 1 2.3-1.6M8.5 13.5a5 5 0 0 1 7 0M12 18h.01"/>',wifi:'<path d="M5 12.5a10 10 0 0 1 14 0M8.5 16a5 5 0 0 1 7 0M2 9a14 14 0 0 1 20 0M12 20h.01"/>',check:'<path d="m20 6-11 11-5-5"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',camera:'<path d="M14.5 4 16 7h4a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V9a2 2 0 0 1 2-2h4l1.5-3z"/><circle cx="12" cy="13" r="4"/>',note:'<path d="M4 4h16v16H4zM8 9h8M8 13h8M8 17h5"/>',arrow:'<path d="m9 18 6-6-6-6"/>',plus:'<path d="M12 5v14M5 12h14"/>',chevLeft:'<path d="m15 18-6-6 6-6"/>',info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v5M12 8h.01"/>',upload:'<path d="M12 16V4M7 9l5-5 5 5M4 20h16"/>',tool:'<path d="M14.7 6.3a4 4 0 0 0-5-5L12 3.6 8.6 7 6.3 4.7a4 4 0 0 0 5 5l-7.6 7.6a2.1 2.1 0 0 0 3 3l7.6-7.6a4 4 0 0 0 5-5L16 11l-3-3z"/>',menu:'<path d="M4 6h16M4 12h16M4 18h16"/>',logout:'<path d="M10 17l5-5-5-5M15 12H3M15 3h5a1 1 0 0 1 1 1v16a1 1 0 0 1-1 1h-5"/>',calendar:'<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 10h18"/>',map:'<path d="m3 6 6-3 6 3 6-3v15l-6 3-6-3-6 3zM9 3v15M15 6v15"/>',filter:'<path d="M4 5h16M7 12h10M10 19h4"/>'};
const icon=(n,c='')=>`<svg class="icon ${c}" viewBox="0 0 24 24" aria-hidden="true">${PATHS[n]||PATHS.info}</svg>`;
let role=sessionStorage.role||'',route=location.hash.slice(1)||'dashboard',tab='checklist',demoOff=false,modal='';const DB='fieldsync',STORES=['inspections','requests','queue','audit','conflicts','evidence','meta'];let db;
async function open(){if(db)return db;db=await new Promise((ok,no)=>{let r=indexedDB.open(DB,1);r.onupgradeneeded=()=>STORES.forEach(s=>r.result.objectStoreNames.contains(s)||r.result.createObjectStore(s,{keyPath:'id'}));r.onsuccess=()=>ok(r.result);r.onerror=()=>no(r.error)});return db}async function data(s){let d=await open();return new Promise((ok,no)=>{let r=d.transaction(s).objectStore(s).getAll();r.onsuccess=()=>ok(r.result);r.onerror=()=>no(r.error)})}async function get(s,id){let d=await open();return new Promise(ok=>{let r=d.transaction(s).objectStore(s).get(id);r.onsuccess=()=>ok(r.result)})}async function put(s,v){let d=await open();return new Promise(ok=>{let r=d.transaction(s,'readwrite').objectStore(s).put(v);r.onsuccess=()=>ok(v)})}const uid=p=>`${p}-${Date.now()}-${Math.random().toString(36).slice(2,6)}`,now=()=>new Date().toISOString();
async function audit(actor,action,detail){await put('audit',{id:uid('A'),at:now(),actor,action,detail})}async function queue(entity,kind,payload={}){await put('queue',{id:uid('O'),entity,kind,payload,actor:'Mithin Krishna',at:now(),status:'Pending'})}
async function seed(){if(await get('meta','seed'))return;const req=[['REQ-001','ABC Juice Machines','Machine #03','Abnormal noise','High','Assigned','Mithin, JP'],['REQ-002','FreshFoods Ltd.','Conveyor #01','Belt misalignment','Medium','In Progress','Nagul'],['REQ-003','ABC Juice Machines','Filling Unit #02','Leakage detected','High','Pending','—'],['REQ-004','Nature Beverages','Cooling System','Temperature high','Medium','Assigned','Madhu'],['REQ-005','FreshFoods Ltd.','Control Panel','Power fluctuation','Low','Completed','JP']].map(x=>({id:x[0],client:x[1],asset:x[2],issue:x[3],priority:x[4],status:x[5],assigned:x[6],date:'22 Sep 2026'}));for(const x of req)await put('requests',x);const base={client:'ABC Juice Machine Manufacturing',checklist:{motor:null,bearing:null,wiring:null,safety:null},measure:{temperature:'',vibration:''},notes:[],history:[],sync:'Available offline'};for(const x of [{id:'INS-001',asset:'Machine #03',issue:'Abnormal noise',priority:'High',status:'In Progress',location:'Production Floor — Section B',time:'Today · 10:00 AM',history:[{by:'JP',at:'2026-09-18T14:12',result:'Operational',note:'Routine check completed'}]},{id:'INS-002',asset:'Conveyor #01',issue:'Belt misalignment',priority:'Medium',status:'In Progress',location:'Production Floor — Section A',time:'Today · 11:30 AM'},{id:'INS-003',asset:'Filling Unit #02',issue:'Leakage detected',priority:'High',status:'Pending',location:'Section C',time:'Today · 2:00 PM'}])await put('inspections',{...structuredClone(base),...x});await put('meta',{id:'seed'});await put('meta',{id:'lastSync',value:'2026-09-22T10:42:00'});await audit('System','Field package synchronized','3 assigned inspections are available locally')}
const online=()=>navigator.onLine&&!demoOff;function toast(t){$('#toast').innerHTML=`<div class="toast">${esc(t)}</div>`;setTimeout(()=>$('#toast').innerHTML='',2400)}function status(v){let c=/complete|pass|synchron|available|operational/i.test(v)?'green':/progress|assigned|online/i.test(v)?'blue':/high|fail|open|attention|conflict/i.test(v)?'red':/pending|local|medium|offline/i.test(v)?'amber':'';return`<span class="status ${c}">${esc(v)}</span>`}function metric(ic,n,l,s,c=''){return`<div class="metric"><div class="metric-icon ${c}">${icon(ic)}</div><div><strong>${n}</strong><b>${l}</b><small>${s}</small></div></div>`}
const cfg={technician:{name:'Mithin Krishna',sub:'Field Technician',nav:[['dashboard','home','Dashboard'],['inspections','clipboard','My Inspections'],['assets','asset','Assets'],['sync','sync','Sync']],secondary:[['notifications','bell','Notifications'],['profile','user','Profile'],['settings','settings','Settings']]},admin:{name:'Mithin Krishna',sub:'Inspection Manager',nav:[['dashboard','home','Dashboard'],['requests','clipboard','Inspection Requests'],['inspections','calendar','Assignments / Inspections'],['clients','building','Clients'],['assets','asset','Assets'],['teams','users','Teams'],['reports','report','Reports'],['conflicts','alert','Conflicts'],['audit','history','Audit Trail']],secondary:[['settings','settings','Settings'],['profile','user','Profile']]},company:{name:'Arun Kumar',sub:'ABC Juice Machines',nav:[['dashboard','home','Dashboard'],['requests','clipboard','My Requests'],['reports','report','Inspection Reports'],['assets','asset','My Assets']],secondary:[['notifications','bell','Notifications'],['profile','user','Profile']]}};
function brand(){return`<div class="wordmark"><img class="official-logo" src="assets/branding/fieldsync-logo.png" alt="FieldSync — Inspect. Sync. Move Forward."></div>`}function login(){return`<main class="login"><section class="login-visual"><div class="login-photo"></div><div class="login-copy">${brand()}<div class="login-message"><h1>Smarter <span>Inspections</span><br>for a Stronger<br>Tomorrow</h1><p>A collaborative, offline-first platform for inspection teams working in factories, plants and remote locations.</p><div class="benefits"><div class="benefit"><span class="benefit-icon">${icon('wifiOff')}</span>Works Offline</div><div class="benefit"><span class="benefit-icon">${icon('users')}</span>Better Team Coordination</div><div class="benefit"><span class="benefit-icon">${icon('history')}</span>Complete Audit Trail</div><div class="benefit"><span class="benefit-icon">${icon('report')}</span>Higher Efficiency</div></div></div><div class="login-quote">“Reliable inspections. Safer operations. Stronger industries.”</div></div></section><section class="login-panel"><div class="login-top">New to FieldSync? &nbsp;<button class="btn btn-secondary">Contact Us</button></div><form class="login-card" id="login"><h1>Welcome Back</h1><p class="login-sub">Sign in to your FieldSync account</p><div class="role-picker"><button type="button" class="role-option active" data-r="technician">${icon('user')}<strong>Inspector</strong><small>Field Technician</small></button><button type="button" class="role-option" data-r="admin">${icon('clipboard')}<strong>Admin</strong><small>Inspection Manager</small></button><button type="button" class="role-option" data-r="company">${icon('building')}<strong>Company</strong><small>Client / Partner</small></button></div><div class="field"><label>Email</label><input type="email" value="mithin@fieldsync.demo" required autocomplete="email"></div><div class="field"><label>Password</label><input type="password" value="fieldsync" required autocomplete="current-password"></div><div class="form-meta"><label><input type="checkbox" checked> Remember me</label><span>Forgot password?</span></div><button class="btn btn-primary btn-full">Sign In ${icon('arrow')}</button><div class="login-note">${icon('map')}<div><strong>Take your inspections anywhere.</strong><small>Your work is saved locally and synchronized when you're back online.</small></div></div></form></section></main>`}
function shell(content){const c=cfg[role],nav=c.nav.map(([r,i,l])=>`<button class="nav-item ${route===r||route.startsWith(r+'/')?'active':''}" data-route="${r}">${icon(i)}<span>${l}</span></button>`).join(''),secondary=c.secondary.map(([r,i,l])=>`<button class="nav-item ${route===r?'active':''}" data-route="${r}">${icon(i)}<span>${l}</span></button>`).join(''),mobile=c.nav.slice(0,4).map(([r,i,l])=>`<button class="${route===r||route.startsWith(r+'/')?'active':''}" data-route="${r}">${icon(i)}${l}</button>`).join('');return`<div class="app-shell"><aside class="sidebar">${brand()}<div class="nav-group-label">${role==='company'?'COMPANY PORTAL':role==='admin'?'OPERATIONS':'FIELD WORKSPACE'}</div><nav class="nav-list">${nav}</nav><div class="sidebar-spacer"></div><nav class="nav-list sidebar-secondary">${secondary}</nav><div class="sidebar-profile"><div class="avatar gold">${c.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><div><strong>${c.name}</strong><small>${c.sub}</small></div></div></aside><main class="main"><header class="topbar"><button class="top-icon mobile-menu-btn" aria-label="Menu">${icon('menu')}</button><img class="mobile-brand-logo" src="assets/branding/fieldsync-logo.png" alt="FieldSync"><div class="searchbox">${icon('search')}<input aria-label="Search" placeholder="Search inspections, assets, requests…"></div><div class="top-spacer"></div>${role==='technician'?`<button class="connectivity ${online()?'':'off'}" id="net"><span class="connectivity-dot"></span><span><strong>${online()?'Online':'Offline'}</strong>${online()?'Ready to sync':'Changes saved locally'}</span></button>`:''}<button class="top-icon" data-route="notifications" aria-label="Notifications">${icon('bell')}</button><div class="top-user"><div class="avatar">${c.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><div><strong>${c.name}</strong><small>${c.sub}</small></div></div></header><div class="content">${content}</div></main><nav class="mobile-nav">${mobile}</nav>${modal}</div>`}
function assetVisual(asset){const name=String(asset).toLowerCase();if(name.includes('conveyor'))return'assets/equipment/conveyor.svg';if(name.includes('filling'))return'assets/equipment/filling.svg';if(name.includes('cooling'))return'assets/equipment/cooling.svg';if(name.includes('control'))return'assets/equipment/control-panel.svg';if(name.includes('#01'))return'assets/equipment/machine-01.svg';if(name.includes('#02'))return'assets/equipment/machine-02.svg';return'assets/equipment/machine-03.svg'}
function inspectionItem(i){return`<div class="inspection-item" data-open="${i.id}"><img class="asset-thumb" src="${assetVisual(i.asset)}" alt="${esc(i.asset)} equipment illustration"><div><strong>${esc(i.asset)}</strong><small>${esc(i.issue)}<br>${esc(i.location)}</small></div><div>${status(i.priority==='High'?'High Priority':i.status)}</div><div><small>${icon('clock')} ${esc(i.time)}</small></div>${icon('arrow')}</div>`}function activity(a,name,text,time,state='online'){return`<div class="activity-item"><div class="avatar ${a==='MK'?'':'gold'}">${a}</div><div class="activity-copy"><strong>${name}</strong><small><span style="color:${state==='offline'?'var(--amber)':'var(--green)'}">●</span> ${text}</small></div><span class="activity-time">${time}</span></div>`}
function workflow(current=4){const steps=['Request','Review','Assignment','Field work','Sync','Result'];return`<div class="surface workflow">${steps.map((s,i)=>`${i?'<span class="workflow-line"></span>':''}<div class="workflow-step ${i<current?'done':i===current?'current':''}"><span class="workflow-num">${i<current?icon('check'):i+1}</span>${s}</div>`).join('')}</div>`}
async function dashboard(){const ins=await data('inspections'),req=await data('requests'),pending=(await data('queue')).filter(x=>x.status==='Pending'),conf=(await data('conflicts')).filter(x=>x.status==='Open');if(role==='technician')return technicianDashboard(ins,pending,conf);if(role==='admin')return adminDashboard(req,conf);return companyDashboard(req)}
function technicianDashboard(ins,pending,conf){return`<section class="hero"><div class="hero-art"></div><div class="hero-content"><div class="hero-eyebrow">Tuesday · 22 September</div><h1>Good Morning, Mithin!</h1><p>You have <b style="color:var(--gold)">${ins.length} inspections</b> assigned today.</p></div></section><section class="metric-grid">${metric('clipboard',ins.length,'Today’s Inspections','2 pending · 1 in progress')}${metric('check',5,'Completed','This week','green')}${metric('sync',pending.length,'Pending Sync',pending.length?'Stored safely on device':'Everything synchronized','blue')}${metric('alert',conf.length,'Conflicts',conf.length?'Review required':'All clear','red')}</section><div class="layout-main"><div class="stack"><section class="surface section priority-panel"><div class="section-head"><div><h2>Today’s Inspections</h2><p>Start with the highest-priority assignment.</p></div><button class="btn btn-ghost" data-route="inspections">View all ${icon('arrow')}</button></div><div class="inspection-list">${ins.map(inspectionItem).join('')}</div></section><section class="surface offline-banner"><span class="banner-icon">${icon(online()?'wifi':'wifiOff')}</span><div><strong>${online()?'Connected — local work is ready':'Offline mode — field package available locally'}</strong><small>${online()?`${pending.length} local changes waiting for demo synchronization.`:'Changes are queued and will remain available after refresh.'}</small></div><button class="btn btn-primary" data-route="sync">${icon('sync')} Open Sync</button></section></div><aside class="stack"><section class="surface section"><div class="section-head"><div><h2>Team Activity</h2><p>Latest state synchronized to this device.</p></div></div><div class="activity-list">${activity('JP','JP','Inspecting Conveyor #01','10:28 AM')}${activity('M','Madhu','Added notes to Filling Unit #02','10:15 AM')}${activity('N','Nagul','Offline · 2 changes pending','9:42 AM','offline')}${activity('MK','Mithin (You)','Inspecting Machine #03','10:24 AM')}</div></section><section class="surface section"><div class="section-head"><h2>Quick Actions</h2></div><div class="quick-actions"><button class="quick-action" data-open="INS-001">${icon('note')}Add Note</button><button class="quick-action" data-open="INS-001">${icon('camera')}Take Photo</button><button class="quick-action" data-route="sync">${icon('sync')}Sync Status</button></div></section></aside></div>`}
function requestTable(req,company=false){return`<div class="data-wrap"><table class="data-table"><thead><tr><th>Request</th>${company?'':'<th>Client</th>'}<th>Asset / Machine</th><th>Issue</th><th>Priority</th><th>Status</th><th>${company?'Requested':'Assigned to'}</th><th></th></tr></thead><tbody>${req.map(r=>`<tr><td><strong>${r.id}</strong></td>${company?'':`<td>${esc(r.client)}</td>`}<td><strong>${esc(r.asset)}</strong></td><td>${esc(r.issue)}</td><td>${status(r.priority)}</td><td>${status(r.status)}</td><td>${company?esc(r.date):esc(r.assigned)}</td><td>${!company&&r.status!=='Completed'?`<button class="btn btn-ghost" data-assign="${r.id}">Assign</button>`:`<button class="btn btn-ghost">View</button>`}</td></tr>`).join('')}</tbody></table></div>`}
function adminDashboard(req,conf){return`<section class="hero facility"><div class="hero-art"></div><div class="hero-content"><div class="hero-eyebrow">Operations overview</div><h1>Good Morning, Mithin!</h1><p>Here’s what needs attention across inspection operations.</p></div></section>${workflow(2)}<section class="metric-grid">${metric('clipboard',req.length,'Total Requests','2 awaiting action')}${metric('users',2,'Needs Assignment','Prepare field packages','blue')}${metric('clock',2,'In Progress','2 teams active')}${metric('alert',conf.length,'Needs Attention',conf.length?'Conflict review required':'No open conflicts','red')}</section><div class="layout-main"><section class="surface section"><div class="section-head"><div><h2>Recent Inspection Requests</h2><p>Review and prepare technician assignments.</p></div><button class="btn btn-primary" id="newreq">${icon('plus')} New Request</button></div>${requestTable(req)}</section><aside class="stack"><section class="surface section"><div class="section-head"><h2>Operational Attention</h2></div><div class="attention-row">${icon('alert')}<div><strong>${conf.length||'No'} conflicts require review</strong><small>Concurrent field values are never overwritten.</small></div></div><div class="attention-row">${icon('clipboard')}<div><strong>2 requests need assignment</strong><small>REQ-003 has no technician.</small></div></div><div class="attention-row">${icon('sync')}<div><strong>1 field package ready</strong><small>Machine #03 · Mithin and JP</small></div></div></section><section class="surface section"><div class="section-head"><h2>Active Teams</h2></div>${activity('MK','Mithin & JP','Machine #03','In progress')}${activity('N','Nagul','Conveyor #01','In progress')}</section></aside></div>`}
function companyDashboard(req){return`<section class="hero company"><div class="hero-art"></div><div class="hero-content"><div class="hero-eyebrow">ABC Juice Machines</div><h1>Welcome Back, Arun!</h1><p>Here’s the latest on your inspection requests.</p></div></section><section class="metric-grid">${metric('clipboard',req.length,'Total Requests','All time')}${metric('check',1,'Completed','Report available','green')}${metric('clock',2,'In Progress','Inspection team assigned','blue')}${metric('alert',2,'Pending','Awaiting review','red')}</section><div class="layout-main"><div class="stack"><section class="surface section"><div class="section-head"><div><h2>My Inspection Requests</h2><p>Simple status and results for your assets.</p></div><button class="btn btn-primary" id="newreq">${icon('plus')} New Request</button></div>${requestTable(req,true)}</section><section class="surface offline-banner"><span class="banner-icon">${icon('check')}</span><div><strong>Machine #03 inspection is in progress</strong><small>Assigned team: Mithin Krishna and JP · Expected today</small></div><button class="btn btn-secondary" data-route="reports">View Reports</button></section></div><aside class="stack"><section class="surface section"><div class="section-head"><h2>Request Status</h2></div><div class="attention-row">${icon('check')}<div><strong>1 completed</strong><small>Inspection report available</small></div></div><div class="attention-row">${icon('clock')}<div><strong>2 in progress</strong><small>Teams are working</small></div></div><div class="attention-row">${icon('clipboard')}<div><strong>2 pending</strong><small>Awaiting admin review</small></div></div></section><section class="surface section"><div class="section-head"><h2>Latest Updates</h2></div><div class="timeline"><div class="timeline-event"><strong>Filling Unit #02 completed</strong><small>12 Sep · 10:24 AM</small><p>Inspection report is ready.</p></div><div class="timeline-event"><strong>Machine #03 assigned</strong><small>10 Sep · 2:15 PM</small><p>Inspection team confirmed.</p></div></div></section></aside></div>`}
async function inspectionWorkspace(id){const i=await get('inspections',id),ev=(await data('evidence')).filter(x=>x.inspection===id),pending=(await data('queue')).filter(x=>x.status==='Pending'&&x.entity===id);if(!i)return`<div class="empty">Inspection not found</div>`;let body='';if(tab==='checklist')body=checklist(i);if(tab==='notes')body=notes(i);if(tab==='evidence')body=evidence(ev);if(tab==='history')body=history(i);return`<div class="workspace-head"><div><button class="back-link" data-route="inspections">${icon('chevLeft')} My Inspections</button><h1>${esc(i.asset)}</h1><p>${esc(i.client)} · ${esc(i.location)}</p></div><div class="workspace-status">${status(i.status)}${status(i.priority+' Priority')}${status(online()?'Online':'Offline')}</div></div><section class="field-banner"><div><strong>${esc(i.issue)} detected</strong><p>${online()?'Connected. Changes save locally first.':'Offline mode. Assigned package is available locally and safe to edit.'}</p></div>${status(`${pending.length} changes pending sync`)}</section><div class="workspace-layout"><section class="surface inspection-workspace"><div class="workspace-section-title"><div><h2>Field Inspection</h2><small>INS-001 · All changes save automatically</small></div>${status(i.sync)}</div><nav class="workspace-tabs">${[['checklist','Checklist'],['notes','Notes'],['evidence','Evidence'],['history','Previous Inspection']].map(([x,l])=>`<button class="${tab===x?'active':''}" data-tab="${x}">${l}</button>`).join('')}</nav><div class="workspace-body">${body}</div><div class="workspace-actions"><button class="btn btn-secondary" data-route="sync">${icon('sync')} Review Sync</button><button class="btn btn-primary" id="complete">${icon('check')} Complete Inspection</button></div></section><aside class="stack"><section class="surface section already"><div class="already-title">${icon('check')}<h2>${i.history.length?'Already Inspected':'No Previous Inspection'}</h2></div>${i.history.length?`<div class="already-result"><strong>${i.history[0].by} · ${new Date(i.history[0].at).toLocaleString()}</strong><small>Result: ${i.history[0].result}</small></div><p class="muted">Latest synchronized inspection known to this device.</p><button class="btn btn-secondary btn-full" data-tab="history">View Previous Inspection</button>`:`<p class="muted">No synchronized history exists on this device.</p>`}</section><section class="surface section"><div class="section-head"><h2>Local Status</h2></div><div class="operation"><div><strong>Connection</strong><small>Current device state</small></div>${status(online()?'Online':'Offline')}</div><div class="operation"><div><strong>Pending changes</strong><small>Saved in IndexedDB</small></div><b>${pending.length}</b></div><div class="operation"><div><strong>Evidence waiting</strong><small>Not uploaded while offline</small></div><b>${ev.length}</b></div></section></aside></div>`}
function checklist(i){const labels={motor:['Motor condition','Visual and operational check'],bearing:['Bearing condition','Listen and inspect for wear'],wiring:['Wiring','Inspect connections and insulation'],safety:['Safety system','Verify guards and emergency stop']};return`<div>${Object.entries(labels).map(([k,v])=>`<div class="check-row"><div class="check-copy"><strong>${v[0]}</strong><small>${v[1]}</small></div><div class="choice">${['pass','attention','fail'].map(x=>`<button class="${x} ${i.checklist[k]===x?'active':''}" data-check="${k}" data-val="${x}">${x[0].toUpperCase()+x.slice(1)}</button>`).join('')}</div></div>`).join('')}</div><div class="measurement-grid"><div class="measurement-card"><label>Temperature</label><div class="measure-input"><input data-measure="temperature" type="number" value="${esc(i.measure.temperature)}" placeholder="0"><span>°C</span></div></div><div class="measurement-card"><label>Vibration</label><div class="measure-input"><input data-measure="vibration" type="number" step=".1" value="${esc(i.measure.vibration)}" placeholder="0.0"><span>mm/s</span></div></div></div>`}
function notes(i){return`<div class="field"><label>Inspection note</label><textarea id="notetxt" rows="4" placeholder="Describe observations, findings, or follow-up work…"></textarea><div class="field-help">Saved locally first and added to the pending operation queue.</div></div><button class="btn btn-primary" id="addnote">${icon('note')} Save Note Locally</button><div style="margin-top:15px">${i.notes.length?i.notes.slice().reverse().map(n=>`<div class="operation"><div><strong>${esc(n.by)}</strong><small>${new Date(n.at).toLocaleString()}</small><p>${esc(n.text)}</p></div>${status('Saved locally')}</div>`).join(''):`<div class="empty">${icon('note')}<br>No inspection notes yet.</div>`}</div>`}
function evidence(ev){return`<div class="field"><label>Photo or supporting evidence</label><input id="file" type="file" accept="image/*" capture="environment"><div class="field-help">Offline evidence remains “Saved locally” until a real upload transport exists.</div></div><button class="btn btn-primary" id="addevidence">${icon('camera')} Save Evidence Locally</button><div style="margin-top:15px">${ev.length?ev.map(x=>`<div class="operation"><div><strong>${esc(x.name)}</strong><small>${new Date(x.at).toLocaleString()} · Mithin Krishna</small></div>${status(x.status)}</div>`).join(''):`<div class="empty">${icon('camera')}<br>No evidence captured.</div>`}</div>`}
function history(i){return i.history.length?i.history.map(h=>`<div class="operation"><div><strong>${new Date(h.at).toLocaleString()}</strong><small>${h.by} · ${h.note}</small></div>${status(h.result)}</div>`).join(''):`<div class="empty">${icon('history')}<br>No previous synchronized inspection on this device.</div>`}
async function listPage(type){const titles={inspections:['Assignments / Inspections',role==='technician'?'Field packages assigned to this device.':'Prepare, assign, and track field inspections.'],requests:[role==='company'?'My Requests':'Inspection Requests',role==='company'?'Track your requested inspections and reports.':'Review incoming requests and prepare assignments.'],assets:[role==='company'?'My Assets':'Assets & Machines','Inspection readiness, recent results, and scheduled work.'],teams:['Teams','Field availability and active assignments.'],clients:['Clients','Companies, facilities, and current inspection work.'],reports:[role==='company'?'Inspection Reports':'Reports','Readable inspection records and findings.'],notifications:['Notifications','Assignments, synchronization, and result updates.'],profile:['Profile','Account and organization details.'],settings:['Settings','Device and workspace preferences.']};const [title,sub]=titles[type]||[type,''];let body='';if(type==='inspections')body=`<section class="surface section"><div class="inspection-list">${(await data('inspections')).map(inspectionItem).join('')}</div></section>`;else if(type==='requests')body=`${role==='admin'?workflow(1):''}<section class="surface section"><div class="section-head"><div><h2>${title}</h2><p>${sub}</p></div><button class="btn btn-primary" id="newreq">${icon('plus')} Create Request</button></div>${requestTable(await data('requests'),role==='company')}</section>`;else if(type==='assets')body=assetList();else if(type==='reports')body=reportView();else if(type==='teams')body=`<section class="surface section"><div class="activity-list">${activity('MK','Mithin Krishna','3 inspections assigned','Available')}${activity('JP','JP','Machine #03 assigned','Available')}${activity('M','Madhu','Filling Unit #02','Available')}${activity('N','Nagul','2 local changes pending','Offline','offline')}</div></section>`;else if(type==='clients')body=`<section class="surface section">${['ABC Juice Machines · 3 active requests','FreshFoods Ltd. · 1 active inspection','Nature Beverages · 1 request pending'].map(x=>`<div class="operation"><div><strong>${x}</strong><small>Industrial client · Coimbatore, TN</small></div>${icon('arrow')}</div>`).join('')}</section>`;else body=`<section class="surface section"><div class="empty">${icon(type==='settings'?'settings':type==='profile'?'user':'bell')}<h3>${title}</h3><p>${sub}</p></div></section>`;return`<div class="page-title"><div><h1>${title}</h1><p>${sub}</p></div></div>${body}`}
function assetList(){const assets=[['Machine #03','Inspection in progress','20 Sep 2026','Attention Required'],['Conveyor #01','Inspection in progress','12 Sep 2026','Operational'],['Filling Unit #02','Pending inspection','05 Sep 2026','Operational'],['Cooling System','Request pending','18 Sep 2026','Attention Required'],['Control Panel','Available','28 Aug 2026','Operational']];return`<section class="surface section"><div class="asset-list">${assets.map(a=>`<div class="asset-row"><img class="asset-thumb" src="${assetVisual(a[0])}" alt="${esc(a[0])} equipment illustration"><div><strong>${a[0]}</strong><small>ABC Juice Machines · Production Floor B</small></div><div><small>Current status</small>${status(a[1])}</div><div><small>Last inspection</small><strong>${a[2]}</strong><small>${a[3]}</small></div><button class="btn btn-secondary">Open Asset</button></div>`).join('')}</div></section>`}
function reportView(){return`<section class="surface section report-sheet"><div class="report-header"><div><span class="hero-eyebrow">Inspection report · INS-005</span><h2>Filling Unit #02</h2><p class="muted">ABC Juice Machines · 12 Sep 2026</p></div>${status('Operational')}</div><div class="report-block"><h3>Inspection Team</h3><p>Madhu · Field Technician</p></div><div class="report-block"><h3>Checklist Summary</h3><p>4 passed · 0 attention · 0 failed</p></div><div class="report-block"><h3>Measurements</h3><p>Temperature 42°C · Vibration 1.8 mm/s</p></div><div class="report-block"><h3>Notes & Evidence</h3><p>No critical findings. 2 evidence photos recorded.</p></div><div class="report-block"><h3>Conflict & Audit Summary</h3><p>No conflicts. 8 auditable events recorded.</p></div></section>`}
async function assignmentPage(reqId){const r=await get('requests',reqId)||await get('requests','REQ-001');return`<div class="page-title"><div><button class="back-link" data-route="requests">${icon('chevLeft')} Inspection Requests</button><h1>Prepare Assignment</h1><p>Review the request, select technicians, and prepare the offline field package.</p></div></div>${workflow(2)}<div class="layout-main"><section class="surface section"><div class="section-head"><div><h2>${r.id} · ${esc(r.asset)}</h2><p>${esc(r.client)} · ${esc(r.issue)}</p></div>${status(r.priority+' Priority')}</div><div class="field"><label>Assign technicians</label>${['Mithin Krishna','JP','Madhu','Nagul'].map((x,i)=>`<label class="operation"><span><strong>${x}</strong><small>${i<2?'Available · Recommended':'Available'}</small></span><input type="checkbox" name="tech" value="${x}" ${i<2?'checked':''}></label>`).join('')}</div><div class="field"><label>Inspection date</label><input type="date" value="2026-09-22"></div><button class="btn btn-primary btn-full" id="prepare">${icon('sync')} Prepare & Sync Assignment</button></section><aside class="surface section"><div class="section-head"><h2>Field Package</h2></div>${['Asset information','Machine checklist','Previous synchronized inspection','Existing notes','Evidence instructions'].map(x=>`<div class="operation"><div><strong>${x}</strong><small>Included for offline access</small></div><span style="color:var(--green)">${icon('check')}</span></div>`).join('')}<div class="honesty-note">${icon('info')}<span>This prepares the package in the local demo store. No remote device transport is claimed.</span></div></aside></div>`}
async function syncCenter(){const q=await data('queue'),pending=q.filter(x=>x.status==='Pending'),ev=await data('evidence'),last=await get('meta','lastSync');const checks=pending.filter(x=>x.kind==='Checklist updated').length,notes=pending.filter(x=>x.kind==='Note added').length;return`<div class="page-title"><div><h1>Sync Center</h1><p>Understand exactly what is stored locally and ready to synchronize.</p></div>${status(online()?'Online':'Offline')}</div><section class="surface section"><div class="sync-summary"><div class="sync-tile"><strong>${online()?'Online':'Offline'}</strong><small>Connection</small></div><div class="sync-tile"><strong>${pending.length}</strong><small>Pending changes</small></div><div class="sync-tile"><strong>${ev.length}</strong><small>Evidence waiting</small></div><div class="sync-tile"><strong>${last?.value?new Date(last.value).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}):'Never'}</strong><small>Last synchronized</small></div></div><div class="surface offline-banner" style="margin-top:17px"><span class="banner-icon">${icon(online()?'wifi':'wifiOff')}</span><div><strong>${online()?'Ready for local demo synchronization':'Offline mode is working'}</strong><small>${online()?`${checks} checklist updates · ${notes} notes · ${ev.length} evidence items`:'Assigned packages and edits remain available in IndexedDB.'}</small></div><button class="btn btn-primary" id="sync" ${!online()||!pending.length?'disabled':''}>${icon('sync')} ${pending.length?'Sync Now':'Everything Synchronized'}</button></div></section><section class="surface section" style="margin-top:18px"><div class="section-head"><div><h2>Pending Operations</h2><p>Local changes recorded on this device.</p></div><button class="btn btn-secondary" id="peer">Simulate JP Conflict</button></div>${q.length?q.slice().reverse().map(x=>`<div class="operation"><div><strong>${esc(x.kind)}</strong><small>${esc(x.entity)} · ${esc(x.actor)} · ${new Date(x.at).toLocaleString()}</small></div>${status(x.status)}</div>`).join(''):`<div class="empty">${icon('check')}<br>No local operations.</div>`}<div class="honesty-note" style="margin-top:14px">${icon('info')}<span><b>Current MVP scope:</b> Sync commits the browser-local demo queue. There is no remote backend or cross-device transport.</span></div></section>`}
async function conflictCenter(){const c=await data('conflicts');return`<div class="page-title"><div><h1>Conflict Center</h1><p>Review competing field values without silently overwriting either technician.</p></div><button class="btn btn-secondary" id="peer">${icon('alert')} Simulate Conflict</button></div><section>${c.length?c.slice().reverse().map(x=>`<article class="surface conflict-card"><div class="conflict-title"><div><span class="hero-eyebrow">Conflict detected · Review required</span><strong>${esc(x.asset)}</strong><small>${esc(x.field)}</small></div>${status(x.status)}</div><div class="conflict-values"><div class="conflict-value"><strong>Mithin Krishna</strong><b>FAIL</b><small>10:32 AM · saved locally</small></div><div class="conflict-value"><strong>JP</strong><b>PASS</b><small>10:35 AM · synchronized edit</small></div></div><p class="conflict-explain">These technicians changed the same field differently. Select the value that should become the reviewed result.</p>${x.status==='Open'?`<div class="modal-actions"><button class="btn btn-secondary" data-resolve="${x.id}" data-v="Fail">Keep Mithin</button><button class="btn btn-primary" data-resolve="${x.id}" data-v="Pass">Keep JP</button></div>`:`<div class="honesty-note">${icon('check')} Resolved by Inspection Admin · Result: ${x.resolution}</div>`}</article>`).join(''):`<div class="surface empty">${icon('check')}<h3>No open conflicts</h3><p>Concurrent field values will appear here for human review.</p></div>`}</section>`}
async function auditTrail(){const a=(await data('audit')).sort((x,y)=>y.at.localeCompare(x.at));return`<div class="page-title"><div><h1>Audit Trail</h1><p>Who changed what, when, and why.</p></div><button class="btn btn-secondary">${icon('filter')} Filter Events</button></div><section class="surface section"><div class="timeline">${a.map(x=>`<div class="timeline-event"><strong>${esc(x.actor)}</strong><small>${new Date(x.at).toLocaleString()}</small><p>${esc(x.action)}${x.detail?` — ${esc(x.detail)}`:''}</p></div>`).join('')}</div></section>`}
function requestModal(){return`<div class="modal-backdrop"><form class="modal" id="reqform"><h2>Create Inspection Request</h2><p>Provide only the information needed to review and assign this inspection.</p><div class="form-grid"><div class="field"><label>Company</label><input name="company" value="ABC Juice Machines" required></div><div class="field"><label>Asset / Machine</label><select name="asset"><option>Machine #03</option><option>Conveyor #01</option><option>Filling Unit #02</option></select></div><div class="field"><label>Priority</label><select name="priority"><option>High</option><option>Medium</option><option>Low</option></select></div><div class="field"><label>Requested inspection date</label><input name="date" type="date" value="2026-09-22"></div></div><div class="field"><label>Issue</label><input name="issue" required placeholder="Example: Abnormal noise"></div><div class="field"><label>Description</label><textarea name="description" rows="3" placeholder="Add context for the inspection team"></textarea></div><div class="modal-actions"><button type="button" class="btn btn-secondary" id="close">Cancel</button><button class="btn btn-primary">Create Inspection Request</button></div></form></div>`}
async function render(){if(!role){$('#app').innerHTML=login();bind();return}let content;if(route==='dashboard')content=await dashboard();else if(route.startsWith('inspection/'))content=await inspectionWorkspace(route.split('/')[1]);else if(route.startsWith('assignment/'))content=await assignmentPage(route.split('/')[1]);else if(route==='sync')content=await syncCenter();else if(route==='conflicts')content=await conflictCenter();else if(route==='audit')content=await auditTrail();else content=await listPage(route);$('#app').innerHTML=shell(content);bind()}
async function updateIns(mut,kind,payload){const i=await get('inspections',route.split('/')[1]);mut(i);i.sync='Pending sync';await put('inspections',i);await queue(i.id,kind,payload);await audit('Mithin Krishna',kind,`${i.asset} · ${Object.values(payload||{}).join(' · ')}`)}function go(r){route=r;location.hash=r;window.scrollTo(0,0);render()}
function bind(){document.querySelectorAll('.role-option').forEach(x=>x.onclick=()=>{document.querySelectorAll('.role-option').forEach(y=>y.classList.remove('active'));x.classList.add('active');$('#login').dataset.role=x.dataset.r});const l=$('#login');if(l)l.onsubmit=e=>{e.preventDefault();role=l.dataset.role||'technician';sessionStorage.role=role;go('dashboard')};document.querySelectorAll('[data-route]').forEach(x=>x.onclick=()=>go(x.dataset.route));document.querySelectorAll('[data-open]').forEach(x=>x.onclick=()=>go(`inspection/${x.dataset.open}`));document.querySelectorAll('[data-assign]').forEach(x=>x.onclick=()=>go(`assignment/${x.dataset.assign}`));document.querySelectorAll('[data-tab]').forEach(x=>x.onclick=()=>{tab=x.dataset.tab;render()});const net=$('#net');if(net)net.onclick=()=>{demoOff=!demoOff;render();toast(online()?'Connection restored':'Offline mode enabled')};document.querySelectorAll('[data-check]').forEach(x=>x.onclick=async()=>{await updateIns(i=>i.checklist[x.dataset.check]=x.dataset.val,'Checklist updated',{field:x.dataset.check,value:x.dataset.val});toast('Checklist saved locally');render()});document.querySelectorAll('[data-measure]').forEach(x=>x.onchange=async()=>{await updateIns(i=>i.measure[x.dataset.measure]=x.value,'Measurement updated',{field:x.dataset.measure,value:x.value});toast('Measurement saved locally')});const an=$('#addnote');if(an)an.onclick=async()=>{const t=$('#notetxt').value.trim();if(!t)return toast('Enter a note first');await updateIns(i=>i.notes.push({by:'Mithin Krishna',at:now(),text:t}),'Note added',{note:t});toast('Note saved locally');render()};const ae=$('#addevidence');if(ae)ae.onclick=async()=>{const f=$('#file').files[0];if(!f)return toast('Choose or capture a photo');await put('evidence',{id:uid('E'),inspection:route.split('/')[1],name:f.name,status:'Saved locally',at:now()});await queue(route.split('/')[1],'Evidence added',{name:f.name});await audit('Mithin Krishna','Evidence saved locally',f.name);toast('Evidence saved locally');render()};const complete=$('#complete');if(complete)complete.onclick=async()=>{await updateIns(i=>i.status='Completed','Inspection completed',{status:'Completed'});toast('Inspection completed and queued');render()};document.querySelectorAll('#sync').forEach(x=>x.onclick=async()=>{if(!online())return toast('Cannot synchronize while offline');x.disabled=true;x.textContent='Synchronizing…';const q=await data('queue');for(const o of q){o.status='Synchronized';await put('queue',o)}for(const i of await data('inspections'))if(i.sync==='Pending sync'){i.sync='Synchronized';await put('inspections',i)}await put('meta',{id:'lastSync',value:now()});await audit('Sync Engine','Synchronization completed',`${q.filter(x=>x.status==='Synchronized').length} operations committed in local demo store`);toast('Local demo synchronization complete');render()});document.querySelectorAll('#peer').forEach(x=>x.onclick=async()=>{let c=(await data('conflicts')).find(x=>x.status==='Open');if(!c){await put('conflicts',{id:uid('C'),asset:'Machine #03',field:'Bearing condition',status:'Open',created:now()});await audit('Sync Engine','Conflict detected','Machine #03 · Bearing condition · Mithin=Fail · JP=Pass')}go('conflicts');toast('Concurrent values preserved for review')});document.querySelectorAll('[data-resolve]').forEach(x=>x.onclick=async()=>{const c=await get('conflicts',x.dataset.resolve);c.status='Resolved';c.resolution=x.dataset.v;await put('conflicts',c);await audit('Inspection Admin','Conflict resolved',`Bearing condition → ${x.dataset.v}`);toast('Conflict resolved and audited');render()});document.querySelectorAll('#newreq').forEach(x=>x.onclick=()=>{modal=requestModal();render()});const close=$('#close');if(close)close.onclick=()=>{modal='';render()};const rf=$('#reqform');if(rf)rf.onsubmit=async e=>{e.preventDefault();const f=new FormData(rf),r={id:`REQ-${String((await data('requests')).length+1).padStart(3,'0')}`,client:f.get('company'),asset:f.get('asset'),issue:f.get('issue'),priority:f.get('priority'),status:'Pending',assigned:'—',date:new Date(f.get('date')).toLocaleDateString()};await put('requests',r);await audit(role==='company'?'Arun Kumar':'Mithin Krishna','Inspection request created',`${r.id} · ${r.asset}`);modal='';toast('Inspection request created');render()};const prep=$('#prepare');if(prep)prep.onclick=async()=>{await audit('Mithin Krishna','Assignment package prepared','REQ-001 · Mithin Krishna and JP · available locally');toast('Local assignment package prepared');go('inspections')}}
window.addEventListener('hashchange',()=>{route=location.hash.slice(1)||'dashboard';window.scrollTo(0,0);render()});window.addEventListener('online',()=>{demoOff=false;render()});window.addEventListener('offline',render);await seed();await render();navigator.serviceWorker?.register('sw.js').catch(console.error);
